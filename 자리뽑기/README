"name.txt"에서 이름들은 ',' 로 각각의 이름들을 구분함(공백은 모두 지움)
가로 세로 크기 설정 후 우클릭으로 안 쓰는 자리를 지워서 실제 책상 배치랑 똑같이 설정
"seatPosition.hwp"에서 원하는 배치 모양을 만든 후 교탁을 기준으로 왼쪽 위가 제일 마지막 번호이고 아래 오른쪽 순으로 -1씩 줄여 위치를 지정함(hwp 파일과 프로그램 이름이 같지 않도록 주의)
